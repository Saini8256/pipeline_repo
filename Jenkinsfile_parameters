pipeline{
    agent any
    parameters {
        booleanParam defaultValue: true, name: 'RUN_PROJECT'
    }
    stages{
        stage('First Stage'){
            steps{
                sh 'sleep 15'
                echo "Pipeline for Jenkins Boolean Parameters"
            }
        }
    }
}